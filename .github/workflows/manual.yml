name: Test workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
    - name: TEST Env
      run: |
        echo "URL=Hello Hau" >> $GITHUB_ENV
        echo "${{ env.URL }}"
